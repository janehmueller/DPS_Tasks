from typing import List


class DateAugmentor(object):

    def augment_dates(self, dates: List[str]) -> List[str]:
        """
        TODO: multiple modes:
        - swap order of elements
        - edge cases of month and year
        - invalid dates
        - dates with letters (e.g. Jan, Feb)
        - other data (e.g. addresses)
        - number data that is not dates but might look similar (telephone numbers)
        """
        # TODO
        # multiple modes
        pass
